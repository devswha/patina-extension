{
  "name": "patina-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "node --check src/content.js && node --check src/local-score.js && node --check src/popup.js && node --check scripts/build.mjs"
  },
  "devDependencies": { "esbuild": "0.28.2" }
}
